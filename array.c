#include <stdio.h>
int main() {
    // declare an array
    int numbers[5];
    // variable store the sum 
    int sum = 0;
    // variable store the average
    float average;

    // prompt the user to enter 5 array
    
    printf("enter the 5 numbers:  \n");

    // read 5 numbers in the array

    for(int i =0; i<5: i++);
    printf("enter the number%d: ",i + 1);
    scanf("%d", $number [i]);
    // calculate the sum 
    for( int i = 0; i < 5; i++)

    // calculate
    
    average=sum/5.0;
    prinf("sum of the numbers: %d\n",sum);

    prinf("average of the numbers: %.2f\n", average)
    
    return 0;
    
    
}