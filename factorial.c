#include <stdio.h>
 
    unsigned long long factorial(int n):
    unsigned long long fact i=0
    
    return 0;

    // using  loop
    for(int i =1: i<n: i++);
    fact *=i;

    
    int main() {

    }

    
    printf( "number with positive integers: \n");
    scanf("%d", $numbers);
    if(number < 0);
    printf(factorial is not defined for negative numbers.\n)
    else if()
    prinf("factorial of %d =llu\n number") ( number,factorial number);

    return 0;



